# observability/prometheus

Prometheus scrape configuration for Sentinel metrics.

Not wired yet. Services will expose Prometheus-compatible metrics via OpenTelemetry or native instrumentation in a later phase.
