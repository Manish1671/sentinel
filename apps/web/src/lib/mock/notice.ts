/**
 * MOCK / DEMO DATA — UI DEVELOPMENT ONLY
 *
 * These fixtures exist so Phase 8A can compose screens before API
 * integration. They are not production telemetry, not a backend, and
 * must not be imported by API clients.
 *
 * Later phases should replace page usage with `lib/api` clients.
 */
export const MOCK_DATA_NOTICE =
  "Development fixtures only. Not live Sentinel data. Do not treat as production telemetry.";

export const IS_MOCK_DATA = true as const;
