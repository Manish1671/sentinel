# Placeholder only. Overlays must supply a real Secret named sentinel-secrets.
# Never commit production values.
apiVersion: v1
kind: Secret
metadata:
  name: sentinel-secrets
  namespace: sentinel
  labels:
    app.kubernetes.io/part-of: sentinel
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
  AUTH_TOKEN_SECRET: "REPLACE_ME_MIN_16_CHARS"
  AI_API_KEY: ""
