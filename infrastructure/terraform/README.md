# infrastructure/terraform

Terraform modules for Sentinel infrastructure.

Later this will provision AWS resources (network, Kubernetes, data stores, IAM) in a reviewable way.

Do not store state files or real `tfvars` in git.
